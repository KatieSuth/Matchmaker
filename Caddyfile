{$DOMAIN} {
    # Backend
    handle /api/* {
        uri strip_prefix /api
        reverse_proxy api:{$API_PORT}
    }

    # Frontend
    handle {
        reverse_proxy frontend:{$FRONTEND_PORT}
    }
}