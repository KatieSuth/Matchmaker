services:
  api:
    build:
      context: ./backend
      dockerfile: Dockerfile
    environment:
      GIN_MODE: release

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    env_file:
      ./frontend/.env